{
  "name": "doodle-fight-model",
  "version": "0.1.0",
  "private": true,
  "description": "The doodle fight judge. Implements the contract Judge port over Gemini.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/server.ts",
    "smoke": "tsx scripts/smoke.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "eval:build": "tsx eval/build.ts",
    "eval:run": "tsx eval/run.ts",
    "eval:report": "tsx eval/report.ts"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "@napi-rs/canvas": "^1.0.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  }
}
