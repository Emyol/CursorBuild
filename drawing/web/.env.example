# Origin of the room server. Leave unset in production when the UI is served
# from the Worker (same origin). Local Vite defaults to http://127.0.0.1:8787.
# VITE_SERVER_URL=https://doodle-fight-rooms.example.workers.dev
